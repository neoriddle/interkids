 <div class="label-field-pair">
    <div class="text-input-bg"><%= select :batch, :batch_id,
      Batch.active.all.map {|c| [c.full_name, c.id]},
      {:prompt => t('app.views.user._student_user.select_a_course')},
      {:onChange => "#{remote_function(:url => {:action => "list_student_user"},
        :with => "'batch_id='+value",
        :before => "Element.show('loader')",
        :success => "Element.hide('loader')"
        )}"} %></div>
  </div>