<% this_user = User.find(session[:user_id]) if session[:user_id] %>
<%= t('app.views.user._header_link.logged_in_as')%>
          <%= link_to this_user.first_name, :controller => 'user', :action => 'profile',
            :id => this_user.username %> &nbsp;|&nbsp;
            <%= link_to t('app.views.user._header_link.messages'),:controller => "reminder", :action => "index" %>(<%= render :partial => 'layouts/reminder_count' %>)&nbsp;|&nbsp;
          <%= link_to t('app.views.user._header_link.log_out'), :controller => 'user', :action => 'logout' %>  &nbsp;&nbsp;