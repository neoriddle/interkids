 <div id="content-header">
    <img src="/images/show_settings.png" />
  <h1><%=  t('app.views.batch_transfers.index.batch_transfer')%></h1>
  <h3><%=  t('app.views.batch_transfers.index.home')%></h3>
  <div id="app-back-button">
    <%= link_to_function image_tag("/images/buttons/back.png",:border => 0), "history.back()" %>
  </div>
</div>

 
<div id="page-yield">
   <% unless flash[:notice].nil? %>
    <p class="flash-msg"> <%= flash[:notice] %> </p>
  <% end %>

 <div class="label-field-pair">
<label><%=  t('app.views.batch_transfers.index.select_a_course')%>
<%= image_tag("loader.gif",:align => "absmiddle",:border => 0,:id => "loader", :style =>"display: none;" ) %>
   </label>
   <div class="text-input-bg">
  <%= select :courses, :name,
        Course.active.map {|b| [b.course_name, b.id]},
        {:prompt => t('app.views.batch_transfers.index.select_a_course')},
        {:onChange => "#{remote_function(:url => {:action => "update_batch"},
        :with => "'course_name='+ value",
        :before => "Element.show('loader')",
        :success => "Element.hide('loader')"
        )}"} %>
</div></div>

<div id="update_batch" style="float:left; width:800px"></div>

<div class="extender"></div>

 </div>