
<div id="fee-collections-list">
<table id="listing">
  <tr class="tr-head">
    <td><%= t('app.views.finance._fee_collection_dates_batch.name') %></td>
    <td><%= t('app.views.finance._fee_collection_dates_batch.start_date') %></td>
    <td><%= t('app.views.finance._fee_collection_dates_batch.end_date') %></td>
    <td><%= t('app.views.finance._fee_collection_dates_batch.due_date') %></td>
    <td></td>
  </tr>

  <% @finance_fee_collections.each do |f| %>
    <tr class="tr-<%= cycle('odd', 'even')%> ">
      <td class="col-3"><%= shorten_string(f.name,17)%></td>
      <td class="col-3"><%= f.start_date %></td>
      <td class="col-3"><%= f.end_date %></td>
      <td class="col-3"><%= f.due_date %></td>
      <td class="col-1">
        <div class="edit"><%= link_to_remote t('app.views.finance._fee_collection_dates_batch.edit'), :url => {:action => 'fee_collection_edit', :id => f.id} %></div>
        <% unless f.check_fee_category == false %>
          <div class="delete"><%= link_to_remote t('app.views.finance._fee_collection_dates_batch.delete'), :url => {:action => 'fee_collection_delete', :id => f.id },:confirm=> t('app.views.finance._fee_collection_dates_batch.question') %></div>
        <% end %>
      </td>
   </tr>
  <% end %>
</table>
</div>